# ============================================================================
# .gitignore - TechGear Project
# ============================================================================

# Dependencies
node_modules/

# Lockfiles:
# - For npm projects: COMMIT package-lock.json (do NOT ignore it)
# - Ignore other lockfiles to avoid mixing package managers
yarn.lock
pnpm-lock.yaml

# Development / IDE
.vscode/
.idea/
*.log
*.tmp
*.swp
*.swo
*~

# OS / System files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini

# Build & Cache
dist/
build/
.cache/
.parcel-cache/
.nyc_output/
coverage/
.eslintcache
.stylelintcache

# Environment (commit only safe templates like .env.example)
.env
.env.*
!.env.example
!.env.template
!.env.sample

# Image / design sources & originals (don't commit unoptimized/source assets)
**/originals/
*.psd
*.ai
*.sketch

# Temporary directories
.tmp/

# Claude Code local-only files (keep CLAUDE.md tracked)
.claude/settings.local.json
.claude/.session.local.json
.claude/.auth.local.json
.claude/.history.local.json
.claude/.cache.local.json
.claude/.temp.local.json
.claude/.logs.local.json
.claude/.data.local.json
